# V1 基础设施与仓库脚手架 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 搭建 zhijin 平台 monorepo 骨架（三服务：Kotlin / Python / React），让三服务都能启动、连通既有 Nacos 并跑通健康检查；中间件全部由既有基础设施提供，本仓库不编排。

**Architecture:** 单仓库（monorepo）承载三个服务：`zhijin-server`（Kotlin + Spring Boot 4 + Spring Cloud Alibaba，Maven 多模块）、`zhijin-ai`（Python + FastAPI）、`zhijin-web`（React + Vite + antd）。**全部中间件（PostgreSQL / Redis / Elasticsearch / Nacos / MinIO）均由既有基础设施提供**，本仓库不编排中间件，服务经环境变量连接。本计划只交付「地基」——服务空壳能跑、能注册、能健康检查，业务逻辑留给计划 B/C/D。

**Tech Stack:** Kotlin 2.2 + Spring Boot 4.0 + Spring Cloud 2025.1 + Spring Cloud Alibaba 2025.1.0.0 (Nacos) · Python 3.11 + FastAPI + uv · React 18 + Vite + TypeScript + antd 5 · Docker Compose

**设计依据:** `docs/superpowers/specs/2026-08-17-agent-platform-design.md` §3（总体架构）、§4（技术选型）、§5（仓库结构）、§6（模块划分）、§12.1（七大编程原则与可扩展性红线）。

---

## 文件结构

本计划将创建的目录与文件：

```
zhijin/                          ← monorepo 根
├── .gitignore
├── README.md                    （已存在，不动）
├── deploy/                      ← 部署目录（新增）
│   ├── .env.example             ← 外部基础设施连接信息样例
│   └── README.md                ← 部署/本地开发/冒烟说明
├── scripts/
│   └── smoke.sh                 ← 冒烟验证脚本
├── zhijin-server/               ← Kotlin 平台服务（Maven 多模块）
│   ├── pom.xml                  ← 根聚合 pom（依赖管理 + 模块清单）
│   └── zhijin-{common,framework,auth,app,orchestrator,chat,tool,billing-audit,ai-client}/
                                  （每模块含 pom.xml；本机 Maven 直接构建，不用 wrapper）
├── zhijin-ai/                   ← Python AI 服务（uv 管理）
│   ├── pyproject.toml           ← uv 项目定义（依赖 + dev 组）
│   ├── uv.lock                  ← 依赖锁文件（uv sync 生成）
│   ├── app/main.py
│   └── ...
└── zhijin-web/                  ← React 前端
    ├── package.json
    ├── vite.config.ts
    └── src/...
```

模块拆分依据设计文档 §6（V1 独立模块：common/framework/app/orchestrator/chat/ai-client；合并模块：auth+tenant、billing+audit；tool 与 orchestrator 协同）。模块边界遵循单一职责（SRP）与依赖倒置（DIP）。

---

## Task 1: monorepo 根骨架

**Files:**
- Create: `C:\mypro\JavaProject\zhijin\.gitignore`
- Create: `C:\mypro\JavaProject\zhijin\deploy\README.md`

- [ ] **Step 1: 创建根 `.gitignore`**

创建 `.gitignore`（在 monorepo 根，覆盖三语言产物）：

```gitignore
# --- 通用 ---
.idea/
*.iml
.DS_Store
*.log

# --- Kotlin/Maven ---
zhijin-server/target/
zhijin-server/**/target/

# --- Python ---
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# --- Node/前端 ---
node_modules/
dist/
*.local

# --- 部署本地环境 ---
deploy/.env
```

- [ ] **Step 2: 创建 `deploy/README.md`**

```markdown
# 部署（deploy）

全部中间件（PostgreSQL / Redis / Elasticsearch / Nacos / MinIO）**均由既有基础设施提供**，本仓库不编排中间件。平台服务的容器化部署（docker-compose）在服务代码就绪后补充（后续计划）。

## 连接配置

复制 `.env.example` 为 `.env`，填入既有基础设施的真实地址（见 Task 2）。

## 本地开发

三个服务本地运行：

| 服务 | 命令 | 端口 |
|---|---|---|
| 平台服务 | `cd zhijin-server && mvn -pl zhijin-app spring-boot:run` | 8080 |
| AI 服务 | `cd zhijin-ai && uv run uvicorn app.main:app --port 8001` | 8001 |
| 前端 | `cd zhijin-web && npm run dev` | 5173 |

## 冒烟

```bash
./scripts/smoke.sh
```
```

- [ ] **Step 3: 验证骨架**

Run: `git status`
Expected: 显示 `.gitignore`、`deploy/README.md` 为新增文件。

- [ ] **Step 4: Commit**

```bash
git add .gitignore deploy/README.md
git commit -m "chore: monorepo 根骨架与部署说明"
```

---

## Task 2: 外部基础设施连接配置

**背景**：PostgreSQL / Redis / Elasticsearch / Nacos / MinIO **全部由既有基础设施提供**，本仓库**不编排任何中间件**。服务通过环境变量 / Nacos 配置连接外部设施。平台服务容器化部署（docker-compose）在服务代码就绪后于后续计划补充。

**Files:**
- Create: `deploy/.env.example`

- [ ] **Step 1: 创建 `deploy/.env.example`**（全部外部基础设施连接信息）

```bash
# ---- 外部既有基础设施（本仓库不编排，全部由既有环境提供） ----
# PostgreSQL（平台主库）
POSTGRES_URL=jdbc:postgresql://<你的PG地址>:5432/zhijin
POSTGRES_USER=zhijin
POSTGRES_PASSWORD=***

# Redis（会话/缓存/限流/异步流）
REDIS_ADDR=127.0.0.1:6379

# Nacos（注册/配置中心）
NACOS_ADDR=127.0.0.1:8848

# Elasticsearch（向量+全文检索）
ES_ADDR=http://127.0.0.1:9200

# MinIO（对象存储，S3 兼容接口）
MINIO_ENDPOINT=http://127.0.0.1:9000
MINIO_ACCESS_KEY=zhijin
MINIO_SECRET_KEY=***
```

- [ ] **Step 2: 复制为 `.env` 并填入真实地址**

Run:
```bash
cd deploy
cp .env.example .env
# 编辑 .env，把 <你的PG地址> 等占位符替换为既有基础设施的真实地址
```
Expected: `.env` 就位，含全部外部设施连接信息。

- [ ] **Step 3: Commit**

```bash
cd ..
git add deploy/
git commit -m "feat(deploy): 外部基础设施连接配置(全部组件由既有环境提供)"
```

---

## Task 3: Kotlin 平台服务空壳（Maven 多模块 + Nacos 注册）

**Files:**
- Create: `zhijin-server/pom.xml`（根聚合 pom：依赖管理 + 模块清单）
- Create: 各模块 `zhijin-*/pom.xml`
- Create: `zhijin-server/zhijin-app/src/main/kotlin/com/zhijin/app/ZhijinApplication.kt`
- Create: `zhijin-server/zhijin-app/src/main/resources/application.yml`

模块骨架（依据 §6，V1 独立/合并后）：
`zhijin-common`（基础）、`zhijin-framework`（基础）、`zhijin-auth`（含 tenant）、`zhijin-app`（启动入口 + 应用管理）、`zhijin-orchestrator`（编排引擎）、`zhijin-chat`（会话）、`zhijin-tool`（工具）、`zhijin-billing-audit`（合并）、`zhijin-ai-client`（调用 Python）。

- [ ] **Step 1: 创建根 `pom.xml`**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.zhijin</groupId>
  <artifactId>zhijin-server</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>pom</packaging>
  <name>zhijin-server</name>

  <modules>
    <module>zhijin-common</module>
    <module>zhijin-framework</module>
    <module>zhijin-auth</module>
    <module>zhijin-app</module>
    <module>zhijin-orchestrator</module>
    <module>zhijin-chat</module>
    <module>zhijin-tool</module>
    <module>zhijin-billing-audit</module>
    <module>zhijin-ai-client</module>
  </modules>

  <properties>
    <java.version>17</java.version>
    <kotlin.version>2.2.0</kotlin.version>
    <spring-boot.version>4.0.0</spring-boot.version>
    <spring-cloud.version>2025.1.0</spring-cloud.version>
    <spring-cloud-alibaba.version>2025.1.0.0</spring-cloud-alibaba.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <repositories>
    <repository>
      <id>aliyun</id>
      <url>https://maven.aliyun.com/repository/public</url>
    </repository>
  </repositories>

  <dependencyManagement>
    <dependencies>
      <!-- 三份 BOM 统一版本管理：Boot / Cloud / Alibaba(Nacos) -->
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.springframework.cloud</groupId>
        <artifactId>spring-cloud-dependencies</artifactId>
        <version>${spring-cloud.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>com.alibaba.cloud</groupId>
        <artifactId>spring-cloud-alibaba-dependencies</artifactId>
        <version>${spring-cloud-alibaba.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <!-- 内部模块版本管理（Maven 要求模块间依赖被 dependencyManagement 管理或显式写版本） -->
      <dependency>
        <groupId>com.zhijin</groupId>
        <artifactId>zhijin-common</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.zhijin</groupId>
        <artifactId>zhijin-framework</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.zhijin</groupId>
        <artifactId>zhijin-auth</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.zhijin</groupId>
        <artifactId>zhijin-orchestrator</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.zhijin</groupId>
        <artifactId>zhijin-chat</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.zhijin</groupId>
        <artifactId>zhijin-tool</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.zhijin</groupId>
        <artifactId>zhijin-billing-audit</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.zhijin</groupId>
        <artifactId>zhijin-ai-client</artifactId>
        <version>${project.version}</version>
      </dependency>
      <!-- Kotlin 标准库版本管理 -->
      <dependency>
        <groupId>org.jetbrains.kotlin</groupId>
        <artifactId>kotlin-stdlib</artifactId>
        <version>${kotlin.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <!-- Kotlin 标准库：所有模块统一继承（Kotlin 2.x Maven 需显式声明） -->
    <dependency>
      <groupId>org.jetbrains.kotlin</groupId>
      <artifactId>kotlin-stdlib</artifactId>
    </dependency>
  </dependencies>

  <build>
    <!-- Kotlin 源码目录约定：所有模块源码放 src/main/kotlin -->
    <sourceDirectory>${project.basedir}/src/main/kotlin</sourceDirectory>
    <testSourceDirectory>${project.basedir}/src/test/kotlin</testSourceDirectory>
    <plugins>
      <!-- Kotlin 编译 + Spring 全开(all-open, Kotlin 2.x 标准用法) -->
      <plugin>
        <groupId>org.jetbrains.kotlin</groupId>
        <artifactId>kotlin-maven-plugin</artifactId>
        <version>${kotlin.version}</version>
        <configuration>
          <jvmTarget>${java.version}</jvmTarget>
          <compilerPlugins>
            <plugin>spring</plugin>
          </compilerPlugins>
        </configuration>
        <dependencies>
          <!-- all-open 编译器插件以依赖形式提供，供 spring preset 使用 -->
          <dependency>
            <groupId>org.jetbrains.kotlin</groupId>
            <artifactId>kotlin-maven-allopen</artifactId>
            <version>${kotlin.version}</version>
          </dependency>
        </dependencies>
        <executions>
          <execution>
            <id>compile</id>
            <phase>compile</phase>
            <goals><goal>compile</goal></goals>
          </execution>
          <execution>
            <id>test-compile</id>
            <phase>test-compile</phase>
            <goals><goal>test-compile</goal></goals>
          </execution>
        </executions>
      </plugin>
      <!-- JUnit5 执行 -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.2.5</version>
      </plugin>
    </plugins>
  </build>
</project>
```

- [ ] **Step 2: 创建各模块 `pom.xml`**

`zhijin-common/pom.xml`（基础模块）：
```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>com.zhijin</groupId>
    <artifactId>zhijin-server</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>zhijin-common</artifactId>

  <dependencies>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.jetbrains.kotlin</groupId>
      <artifactId>kotlin-test-junit5</artifactId>
      <version>${kotlin.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
```

`zhijin-framework/pom.xml`（依赖 common，装 Nacos 配置/发现、Actuator）：
```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>com.zhijin</groupId>
    <artifactId>zhijin-server</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>zhijin-framework</artifactId>

  <dependencies>
    <dependency>
      <groupId>com.zhijin</groupId>
      <artifactId>zhijin-common</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <dependency>
      <groupId>com.alibaba.cloud</groupId>
      <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
    </dependency>
    <dependency>
      <groupId>com.alibaba.cloud</groupId>
      <artifactId>spring-cloud-starter-alibaba-nacos-config</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
```

其余 6 个业务模块 pom 与上述结构一致，仅 `<artifactId>` 不同，且都依赖 common + framework：
`zhijin-auth` / `zhijin-orchestrator` / `zhijin-chat` / `zhijin-tool` / `zhijin-billing-audit` / `zhijin-ai-client`

以 `zhijin-auth/pom.xml` 为例（其余把 `<artifactId>` 换成对应模块名）：
```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>com.zhijin</groupId>
    <artifactId>zhijin-server</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>zhijin-auth</artifactId>

  <dependencies>
    <dependency>
      <groupId>com.zhijin</groupId>
      <artifactId>zhijin-common</artifactId>
    </dependency>
    <dependency>
      <groupId>com.zhijin</groupId>
      <artifactId>zhijin-framework</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
```

`zhijin-app/pom.xml`（可执行模块，聚合所有模块 + Web + fat-jar 打包）：
```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>com.zhijin</groupId>
    <artifactId>zhijin-server</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>zhijin-app</artifactId>

  <dependencies>
    <dependency><groupId>com.zhijin</groupId><artifactId>zhijin-common</artifactId></dependency>
    <dependency><groupId>com.zhijin</groupId><artifactId>zhijin-framework</artifactId></dependency>
    <dependency><groupId>com.zhijin</groupId><artifactId>zhijin-auth</artifactId></dependency>
    <dependency><groupId>com.zhijin</groupId><artifactId>zhijin-orchestrator</artifactId></dependency>
    <dependency><groupId>com.zhijin</groupId><artifactId>zhijin-chat</artifactId></dependency>
    <dependency><groupId>com.zhijin</groupId><artifactId>zhijin-tool</artifactId></dependency>
    <dependency><groupId>com.zhijin</groupId><artifactId>zhijin-billing-audit</artifactId></dependency>
    <dependency><groupId>com.zhijin</groupId><artifactId>zhijin-ai-client</artifactId></dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!-- 打成可执行 fat-jar -->
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <version>${spring-boot.version}</version>
        <executions>
          <execution>
            <goals><goal>repackage</goal></goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
```

> 说明：版本统一由根 pom 的 3 份 BOM 管理，各模块只声明依赖不写版本，符合依赖倒置与单一职责；可执行 jar 只在 `zhijin-app` 生成。

- [ ] **Step 4: 创建 Spring Boot 启动类**

`zhijin-app/src/main/kotlin/com/zhijin/app/ZhijinApplication.kt`：
```kotlin
package com.zhijin.app

import org.springframework.boot.autoconfigure.SpringBootApplication
import org.springframework.boot.runApplication

/**
 * 平台服务启动入口（Kotlin + Spring Boot 4）。
 *
 * 注解说明：
 * - scanBasePackages 覆盖全部 zhijin 模块，各模块通过 Spring 组件扫描装配。
 */
@SpringBootApplication(scanBasePackages = ["com.zhijin"])
class ZhijinApplication

fun main(args: Array<String>) {
    runApplication<ZhijinApplication>(*args)
}
```

- [ ] **Step 5: 创建配置 `application.yml`**

`zhijin-app/src/main/resources/application.yml`：
```yaml
server:
  port: 8080

spring:
  application:
    name: zhijin-server
  config:
    import: optional:nacos:zhijin-server.yml?group=DEFAULT_GROUP&refreshEnabled=true
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDR:127.0.0.1:8848}
        namespace: ${NACOS_NAMESPACE:}
      config:
        server-addr: ${NACOS_ADDR:127.0.0.1:8848}
        namespace: ${NACOS_NAMESPACE:}
        file-extension: yml
```

- [ ] **Step 6: 构建**

Run（本机已装 Maven，直接用 `mvn`；JDK 17，Boot 4 最低要求）：
```bash
cd zhijin-server
mvn clean package
```
Expected: `BUILD SUCCESS`，9 个模块全部编译通过，`zhijin-app/target/zhijin-app-0.1.0-SNAPSHOT.jar` 生成。

- [ ] **Step 7: 启动并验证注册到 Nacos**

Run（中间件需已启动）：
```bash
java -jar zhijin-app/target/zhijin-app-0.1.0-SNAPSHOT.jar
```
另开终端：
```bash
curl "http://localhost:8080/actuator/health"
```
Expected: `{"status":"UP"}`。

```bash
curl "http://localhost:8848/nacos/v1/ns/instance/list?serviceName=zhijin-server"
```
Expected: `hosts` 数组含本机 `8080` 实例。

- [ ] **Step 8: Commit**

```bash
cd ..
git add zhijin-server/
git commit -m "feat(server): Kotlin 平台服务 Maven 多模块骨架 + Nacos 注册连通"
```

---

## Task 4: Python AI 服务空壳

**Files:**
- Create: `zhijin-ai/pyproject.toml`（uv 项目定义）
- Create: `zhijin-ai/app/__init__.py`
- Create: `zhijin-ai/app/main.py`
- Create: `zhijin-ai/app/config.py`
- Create: `zhijin-ai/tests/test_health.py`

- [ ] **Step 1: 创建 `pyproject.toml`**（uv 管理，含 dev 依赖组）

```toml
[project]
name = "zhijin-ai"
version = "0.1.0"
description = "织锦 AI 服务：模型网关 / RAG / 评测"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.111.0",
    "uvicorn[standard]>=0.30.1",
    "httpx>=0.27.0",
    "nacos-sdk-python>=0.4.1",
]

[dependency-groups]
dev = [
    "pytest>=8.2.1",
]

[tool.pytest.ini_options]
pythonpath = ["."]
```

- [ ] **Step 2: 创建 `app/config.py`**（Nacos Open API 拉取，失败回退环境变量）

> 实现说明：当前可安装的 `nacos-sdk-python`（3.x / v2 SDK）为异步客户端，会创建后台 gRPC 线程，在短生命周期进程（测试、空壳启动）中会导致进程无法正常退出。因此 V1 空壳改用 **Nacos Open API（HTTP）** 拉取配置；SDK 依赖保留在 pyproject，供计划 C 正式接入。

```python
"""AI 服务配置：优先从 Nacos 拉取，失败回退环境变量。

说明：当前可安装的 nacos-sdk-python（3.x / v2 SDK）为异步客户端，会创建后台
gRPC 线程，在短生命周期进程（测试、空壳启动）中会导致进程无法正常退出。
因此 V1 空壳改用 Nacos Open API（HTTP）拉取配置；sdk 依赖按计划保留，供
计划 C 正式接入时使用。
"""
import os

import httpx

_NACOS_SERVER = os.getenv("NACOS_ADDR", "127.0.0.1:8848")
_SERVICE_NAME = "zhijin-ai"
_DATA_ID = "zhijin-ai.yml"
_GROUP = "DEFAULT_GROUP"
# 请求超时上限：空壳阶段拉取配置属尽力而为，超时即回退本地默认配置。
_NACOS_TIMEOUT_SECONDS = 2.0


def load_config() -> dict:
    """返回服务配置字典。Nacos 不可用时使用本地默认值，保证空壳可独立启动。"""
    default = {
        "app.name": _SERVICE_NAME,
        "log.level": os.getenv("LOG_LEVEL", "INFO"),
    }
    try:
        # 通过 Nacos Open API 拉取配置：GET /nacos/v1/cs/configs?dataId=X&group=Y
        resp = httpx.get(
            f"http://{_NACOS_SERVER}/nacos/v1/cs/configs",
            params={"dataId": _DATA_ID, "group": _GROUP},
            timeout=_NACOS_TIMEOUT_SECONDS,
        )
        if resp.status_code == 200 and resp.text:
            # V1 空壳仅打日志证明连通；完整解析留给计划 C。
            print(f"[config] 已从 Nacos 拉取 {_DATA_ID}: {len(resp.text)} chars")
        else:
            print(f"[config] Nacos 返回状态 {resp.status_code}，使用本地默认配置")
    except Exception as exc:  # noqa: BLE001 - 空壳阶段 Nacos 不可用不致命
        print(f"[config] Nacos 不可用，使用本地默认配置: {exc}")
    return default
```

- [ ] **Step 3: 创建 `app/main.py`**

```python
"""AI 服务（FastAPI）入口。V1 空壳：仅健康检查，模型网关在计划 C 实现。"""
from fastapi import FastAPI

from .config import load_config

app = FastAPI(title="zhijin-ai", version="0.1.0")

config = load_config()


@app.get("/health")
async def health() -> dict:
    """健康检查端点，供平台服务与编排探活。"""
    return {"status": "UP", "app": config["app.name"]}
```

- [ ] **Step 4: 创建 `tests/test_health.py`**（TDD：先写失败测试）

```python
from fastapi.testclient import TestClient

from app.main import app

client = TestClient(app)


def test_health_returns_up() -> None:
    """健康检查端点必须返回 UP。"""
    resp = client.get("/health")
    assert resp.status_code == 200
    assert resp.json()["status"] == "UP"
```

- [ ] **Step 5: 运行测试确认先失败**

Run（在 `zhijin-ai` 下，`uv sync` 自动建 `.venv` 并安装依赖）：
```bash
cd zhijin-ai
uv sync
uv run pytest tests/test_health.py
```
Expected: FAIL（`app.main` 尚不存在）。

- [ ] **Step 6: 运行测试确认通过**

Run: `uv run pytest tests/test_health.py -v`
Expected: `1 passed`。

- [ ] **Step 7: 启动并验证 /health**

Run:
```bash
uv run uvicorn app.main:app --host 0.0.0.0 --port 8001
```
另开终端：
```bash
curl http://localhost:8001/health
```
Expected: `{"status":"UP","app":"zhijin-ai"}`。

- [ ] **Step 8: Commit**

```bash
cd ..
git add zhijin-ai/
git commit -m "feat(ai): Python AI 服务空壳(FastAPI + /health)"
```

---

## Task 5: React 前端空壳

**Files:**
- Create: `zhijin-web/package.json`
- Create: `zhijin-web/vite.config.ts`
- Create: `zhijin-web/tsconfig.json`
- Create: `zhijin-web/index.html`
- Create: `zhijin-web/src/main.tsx`
- Create: `zhijin-web/src/App.tsx`

- [ ] **Step 1: 创建 `package.json`**

```json
{
  "name": "zhijin-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "antd": "^5.17.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.2",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.2.1",
    "typescript": "^5.4.5",
    "vite": "^5.2.11"
  }
}
```

- [ ] **Step 2: 创建 `vite.config.ts`**

```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

// 开发期把 /api 代理到平台服务(8080)，前端不直连后端其它端口，符合单一入口原则。
export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    proxy: {
      '/api': { target: 'http://localhost:8080', changeOrigin: true },
    },
  },
});
```

- [ ] **Step 3: 创建 `tsconfig.json`**

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true
  },
  "include": ["src"]
}
```

- [ ] **Step 4: 创建 `index.html` 与入口**

`zhijin-web/index.html`：
```html
<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>织锦 · zhijin 控制台</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

`zhijin-web/src/main.tsx`：
```tsx
import React from 'react';
import ReactDOM from 'react-dom/client';
import { ConfigProvider } from 'antd';
import zhCN from 'antd/locale/zh_CN';
import App from './App';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ConfigProvider locale={zhCN}>
      <App />
    </ConfigProvider>
  </React.StrictMode>,
);
```

`zhijin-web/src/App.tsx`：
```tsx
import { Card, Typography } from 'antd';

// V1 空壳：控制台占位页。应用管理、画布编排在计划 D 实现。
export default function App() {
  return (
    <Card style={{ maxWidth: 640, margin: '80px auto' }}>
      <Typography.Title level={3}>织锦 · zhijin 控制台</Typography.Title>
      <Typography.Paragraph>
        企业级智能体平台。V1 脚手架占位，应用管理与编排画布即将上线。
      </Typography.Paragraph>
    </Card>
  );
}
```

- [ ] **Step 5: 安装依赖并构建**

Run:
```bash
cd zhijin-web
npm install
npm run build
```
Expected: `npm run build` 成功（`tsc` 无类型错误 + `vite build` 产出 `dist/`）。

- [ ] **Step 6: 启动开发服务器验证**

Run: `npm run dev`
另开终端：`curl http://localhost:5173/`
Expected: 返回 HTML（含 `<div id="root">`）。

- [ ] **Step 7: Commit**

```bash
cd ..
git add zhijin-web/
git commit -m "feat(web): React 前端空壳(Vite + antd 控制台占位)"
```

---

## Task 6: 全栈冒烟验证

**Files:**
- Create: `scripts/smoke.sh`

- [ ] **Step 1: 创建冒烟脚本**

`scripts/smoke.sh`（Git Bash / Linux 可执行）：
```bash
#!/usr/bin/env bash
# zhijin 全栈冒烟：校验三服务健康端点。
# 说明：全部中间件（PG/Redis/ES/Nacos/MinIO）均为外部既有基础设施，不在此冒烟范围内。
set -euo pipefail

SERVER="${SERVER_ADDR:-127.0.0.1:8080}"
AI="${AI_ADDR:-127.0.0.1:8001}"
WEB="${WEB_ADDR:-127.0.0.1:5173}"

echo "[1/3] 平台服务(8080)"
curl -fs "http://${SERVER}/actuator/health" && echo

echo "[2/3] AI 服务(8001)"
curl -fs "http://${AI}/health" && echo

echo "[3/3] 前端(5173)"
curl -fs "http://${WEB}/" > /dev/null && echo "前端 UP"

echo "== 冒烟通过 =="
```

- [ ] **Step 2: 授权并运行**

Run:
```bash
chmod +x scripts/smoke.sh
./scripts/smoke.sh
```
Expected: 三项全部输出成功，结尾打印 `== 冒烟通过 ==`。

- [ ] **Step 3: Commit**

```bash
git add scripts/smoke.sh
git commit -m "feat(scripts): 全栈冒烟验证脚本"
```

---

## Self-Review 记录

- **Spec 覆盖**：§3 两服务 + 全部中间件外部提供 ✓（Task 2 连接配置）· §4 选型 ✓（Kotlin/Maven/Python+uv/React+TS，PG/Redis/ES/MinIO/Nacos 均为外部既有）· §5 monorepo ✓（Task 1）· §6 模块划分 ✓（Task 3 模块骨架）· §12.1 七大原则 ✓（模块边界 SRP、框架依赖 DIP、启动类单一职责）。
- **占位符扫描**：无 "TBD/TODO/稍后实现"；每步含完整代码或命令。
- **类型一致性**：`/health`（Python）与 `/actuator/health`（Kotlin）端点命名在各任务间一致；服务名 `zhijin-server` / `zhijin-ai` 在 Nacos 与配置间一致。

---

## 执行交接

计划 A（地基）完成后，依次推进：计划 B（平台服务 Kotlin：多租户/认证/应用/编排引擎/会话/AI-client）→ 计划 C（AI 服务：模型网关）→ 计划 D（前端：应用管理 + 最小画布）。
