# V1 平台服务 · B3 应用管理 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `zhijin-app` 模块实现应用（智能体）管理：CRUD + 模型配置（供应商 Key 加密存储）+ 版本发布（draft→published 快照）+ API Key 管理（哈希存储）。

**Architecture:** 业务接口走 `/api/apps/**`，由 B2 的资源服务器链鉴权（Bearer JWT），租户上下文由 B2 的 `JwtTenantFilter` 从 JWT claim 收敛（自动注入 `tenant_id`）。数据层用 B1 的 MyBatis-Plus + 租户拦截器（所有业务表自动 `WHERE tenant_id = ?`）。模型 Key 按决策 21：平台服务持有、AES-256-GCM 加密落库、调用时解密下发（本计划只做存储，实际下发在 B5/计划 C）。API Key 生成时明文展示一次，DB 存 SHA-256 哈希，供后续开放 API 鉴权用（B5 接入）。

**Tech Stack:** Spring Boot 4 · MyBatis-Plus（复用 B1/B2）· Flyway V3 · Spring Security JWT（复用 B2）· H2（测试）

**设计依据:** `2026-08-17-agent-platform-design.md` §6（zhijin-app）、§10 数据流 A、§13 决策 21/28、§12.1 七大原则。

---

## 关键决策

- **应用 = 智能体**：`app` 表承载一个智能体的元数据与生命周期（draft→published→offline）。
- **模型配置**：应用引用 `model_provider_key`（租户级供应商 Key 池）+ 选择模型；`app_model_config` 存应用级模型选择（provider/model_name/key 引用）。
- **供应商 Key 加密**：`model_provider_key.encrypted_key` 用 AES-256-GCM 加密（密钥来自 `MODEL_KEY_SECRET` 环境变量），符合决策 21「平台持有加密、AI 服务不落盘」。
- **API Key**：`app_api_key` 存 SHA-256 哈希；创建时明文返回一次。客户系统调开放 API 时用它识别租户+应用（B5 实现鉴权过滤器）。
- **版本发布**：`app_version` 为不可变快照（version_no 自增，含发布时的工作流 DSL + 模型配置快照）；画布/工作流 DSL 本体在 B4 引入，本计划 `workflow_dsl` 字段先存在、内容为空。
- **CRUD 校验**：编辑/发布/生成 Key 需应用属于当前租户（租户拦截器自动保证行级隔离）。

---

## 文件结构

```
zhijin-server/zhijin-app/src/main/kotlin/com/zhijin/app/
├── entity/App.kt / AppModelConfig.kt / AppVersion.kt / AppApiKey.kt / ModelProviderKey.kt
├── mapper/AppMapper.kt / AppModelConfigMapper.kt / AppVersionMapper.kt / AppApiKeyMapper.kt / ModelProviderKeyMapper.kt
├── dto/AppRequest.kt / AppResponse.kt / ModelConfigRequest.kt / ModelConfigResponse.kt /
│        PublishRequest.kt / ApiKeyResponse.kt / ProviderKeyRequest.kt
├── service/AppService.kt / ModelConfigService.kt / AppApiKeyService.kt / CryptoService.kt
├── controller/AppController.kt
└── (resources) db/migration/V3__app_schema.sql
```

---

## Task 1: 数据模型 + Flyway V3

**Files:**
- Create: `zhijin-server/zhijin-app/src/main/resources/db/migration/V3__app_schema.sql`
- Create: 5 个实体 + 5 个 Mapper（`zhijin-app` 下）

- [ ] **Step 1: 建表 SQL**

`V3__app_schema.sql`：
```sql
-- 应用（智能体）管理相关表
-- 全部业务表带 tenant_id，由 MyBatis-Plus 租户拦截器自动隔离。

CREATE TABLE IF NOT EXISTS app (
    id          BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    tenant_id   BIGINT       NOT NULL,
    app_key     VARCHAR(64)  NOT NULL UNIQUE,          -- 对外标识
    name        VARCHAR(128) NOT NULL,
    description TEXT         NOT NULL DEFAULT '',
    icon_uri    VARCHAR(256) NOT NULL DEFAULT '',
    status      SMALLINT     NOT NULL DEFAULT 0,       -- 0草稿 1已发布 2已下线
    create_by   BIGINT,
    create_time TIMESTAMP    NOT NULL DEFAULT now(),
    update_time TIMESTAMP    NOT NULL DEFAULT now()
);
CREATE INDEX IF NOT EXISTS idx_app_tenant ON app (tenant_id);

-- 模型供应商 Key（租户级，加密存储）
CREATE TABLE IF NOT EXISTS model_provider_key (
    id           BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    tenant_id    BIGINT       NOT NULL,
    provider     VARCHAR(32)  NOT NULL,                -- qwen/claude/openai/deepseek
    name         VARCHAR(128) NOT NULL,
    encrypted_key TEXT        NOT NULL,                -- AES-256-GCM 加密后的密钥
    status       SMALLINT     NOT NULL DEFAULT 1,
    create_time  TIMESTAMP    NOT NULL DEFAULT now(),
    update_time  TIMESTAMP    NOT NULL DEFAULT now()
);
CREATE INDEX IF NOT EXISTS idx_mpk_tenant_provider ON model_provider_key (tenant_id, provider);

-- 应用模型配置
CREATE TABLE IF NOT EXISTS app_model_config (
    id             BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    tenant_id      BIGINT       NOT NULL,
    app_id         BIGINT       NOT NULL,
    provider       VARCHAR(32)  NOT NULL,
    model_name     VARCHAR(128) NOT NULL,
    provider_key_id BIGINT,                            -- 引用 model_provider_key.id
    temperature    DOUBLE PRECISION NOT NULL DEFAULT 0.7,
    max_tokens     INT          NOT NULL DEFAULT 2048,
    create_time    TIMESTAMP    NOT NULL DEFAULT now(),
    update_time    TIMESTAMP    NOT NULL DEFAULT now()
);
CREATE UNIQUE INDEX IF NOT EXISTS uk_amc_app ON app_model_config (tenant_id, app_id);

-- 应用版本（发布快照）
CREATE TABLE IF NOT EXISTS app_version (
    id           BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    tenant_id    BIGINT       NOT NULL,
    app_id       BIGINT       NOT NULL,
    version_no   INT          NOT NULL,                -- 版本号，自增
    workflow_dsl TEXT,                                 -- 工作流 DSL（B4 填充）
    model_snapshot TEXT,                               -- 模型配置快照(JSON)
    status       SMALLINT     NOT NULL DEFAULT 1,      -- 1已发布
    publish_by   BIGINT,
    publish_time TIMESTAMP,
    create_time  TIMESTAMP    NOT NULL DEFAULT now()
);
CREATE UNIQUE INDEX IF NOT EXISTS uk_av_app_version ON app_version (tenant_id, app_id, version_no);

-- 应用 API Key（哈希存储）
CREATE TABLE IF NOT EXISTS app_api_key (
    id          BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    tenant_id   BIGINT       NOT NULL,
    app_id      BIGINT       NOT NULL,
    key_hash    VARCHAR(64)  NOT NULL,                 -- SHA-256 hex
    name        VARCHAR(128) NOT NULL DEFAULT '',
    status      SMALLINT     NOT NULL DEFAULT 1,       -- 1启用 0吊销
    create_time TIMESTAMP    NOT NULL DEFAULT now(),
    expire_time TIMESTAMP
);
CREATE INDEX IF NOT EXISTS idx_aak_app ON app_api_key (tenant_id, app_id);
```

- [ ] **Step 2: 实体（Kotlin data class，带 @TableName / 自动填充注解）**

以 `App.kt` 为例（其余 4 个同风格，字段对应表）：
```kotlin
package com.zhijin.app.entity

import com.baomidou.mybatisplus.annotation.FieldFill
import com.baomidou.mybatisplus.annotation.IdType
import com.baomidou.mybatisplus.annotation.TableField
import com.baomidou.mybatisplus.annotation.TableId
import com.baomidou.mybatisplus.annotation.TableName
import java.time.LocalDateTime

/** 智能体应用实体（对应 app 表）。 */
@TableName("app")
data class App(
    @TableId(type = IdType.AUTO)
    var id: Long? = null,
    var tenantId: Long? = null,
    var appKey: String = "",
    var name: String = "",
    var description: String = "",
    var iconUri: String = "",
    var status: Int = 0,
    var createBy: Long? = null,
    @TableField(fill = FieldFill.INSERT)
    var createTime: LocalDateTime? = null,
    @TableField(fill = FieldFill.INSERT_UPDATE)
    var updateTime: LocalDateTime? = null,
)
```
`ModelProviderKey` / `AppModelConfig` / `AppVersion` / `AppApiKey` 同理（字段按表定义）。

- [ ] **Step 3: Mapper（5 个）**

以 `AppMapper.kt` 为例（其余 4 个同风格）：
```kotlin
package com.zhijin.app.mapper

import com.baomidou.mybatisplus.core.mapper.BaseMapper
import com.zhijin.app.entity.App
import org.apache.ibatis.annotations.Mapper

@Mapper
interface AppMapper : BaseMapper<App>
```
> 注意：`@MapperScan` 需覆盖 `com.zhijin.app.mapper`。B2 的 `MybatisPlusConfig` 目前扫描 `com.zhijin.framework.mapper` + `com.zhijin.auth.repository`。需追加 `com.zhijin.app.mapper`（改 `@MapperScan`，这是必要修改）。

- [ ] **Step 4: 构建验证**

Run: `cd C:\mypro\JavaProject\zhijin\zhijin-server && mvn -pl zhijin-app -am clean compile`
Expected: `BUILD SUCCESS`（迁移在启动时才执行，编译不连库）。

- [ ] **Step 5: Commit**

```bash
cd C:\mypro\JavaProject\zhijin
git add zhijin-server/zhijin-app/ zhijin-server/zhijin-framework/src/main/kotlin/com/zhijin/framework/tenant/MybatisPlusConfig.kt
git commit -m "feat(app): 应用管理数据模型(Flyway V3) + 实体/Mapper"
```
（末尾加 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`；不动 `zhijin.iml`。）

---

## Task 2: 应用 CRUD（TDD）

**Files:**
- Create: `zhijin-server/zhijin-app/src/main/kotlin/com/zhijin/app/dto/AppRequest.kt` / `AppResponse.kt`
- Create: `zhijin-server/zhijin-app/src/main/kotlin/com/zhijin/app/service/AppService.kt`
- Create: `zhijin-server/zhijin-app/src/main/kotlin/com/zhijin/app/controller/AppController.kt`
- Test: `zhijin-server/zhijin-app/src/test/kotlin/com/zhijin/app/AppServiceTest.kt`

- [ ] **Step 1: 写失败测试**

`AppServiceTest.kt`（用 mock 仓储验证创建/查询/编辑/删除）：
```kotlin
package com.zhijin.app

import com.zhijin.app.dto.AppRequest
import com.zhijin.app.entity.App
import com.zhijin.app.mapper.AppMapper
import com.zhijin.app.service.AppService
import com.zhijin.common.exception.BizException
import org.junit.jupiter.api.Assertions.assertEquals
import org.junit.jupiter.api.Assertions.assertNotNull
import org.junit.jupiter.api.Assertions.assertThrows
import org.junit.jupiter.api.Test
import org.mockito.Mockito.mock
import org.mockito.Mockito.`when`

class AppServiceTest {

    private val mapper = mock(AppMapper::class.java)
    private val service = AppService(mapper)

    @Test
    fun `创建应用生成appKey并返回`() {
        val app = App(id = 1L, tenantId = 1L, appKey = "app_abc", name = "客服助手")
        `when`(mapper.insert(app)).thenReturn(1)
        val created = service.create(1L, AppRequest(name = "客服助手", description = "售前咨询"))
        assertNotNull(created.id)
        assert(created.appKey.startsWith("app_"))
    }

    @Test
    fun `查询不存在应用抛业务异常`() {
        `when`(mapper.selectById(99L)).thenReturn(null)
        assertThrows(BizException::class.java) { service.get(1L, 99L) }
    }
}
```

- [ ] **Step 2: 运行确认失败**

Run: `cd C:\mypro\JavaProject\zhijin\zhijin-server && mvn -pl zhijin-app test -Dtest=AppServiceTest`
Expected: 编译失败（类不存在）。

- [ ] **Step 3: 实现**

`dto/AppRequest.kt`：
```kotlin
package com.zhijin.app.dto

import jakarta.validation.constraints.NotBlank
import jakarta.validation.constraints.Size

data class AppRequest(
    @field:NotBlank(message = "应用名不能为空")
    @field:Size(max = 128)
    val name: String = "",
    val description: String = "",
    val iconUri: String = "",
)
```

`dto/AppResponse.kt`：
```kotlin
package com.zhijin.app.dto

data class AppResponse(
    val id: Long,
    val appKey: String,
    val name: String,
    val description: String,
    val iconUri: String,
    val status: Int,
)
```

`service/AppService.kt`：
```kotlin
package com.zhijin.app.service

import com.zhijin.app.dto.AppRequest
import com.zhijin.app.dto.AppResponse
import com.zhijin.app.entity.App
import com.zhijin.app.mapper.AppMapper
import com.zhijin.common.exception.BizException
import com.zhijin.common.web.ResultCode
import org.springframework.stereotype.Service
import java.util.UUID

/** 应用管理：CRUD。租户隔离由 MyBatis-Plus 拦截器自动保证。 */
@Service
class AppService(private val appMapper: AppMapper) {

    fun create(tenantId: Long, req: AppRequest): AppResponse {
        val app = App(
            tenantId = tenantId,
            appKey = "app_" + UUID.randomUUID().toString().replace("-", "").take(16),
            name = req.name,
            description = req.description,
            iconUri = req.iconUri,
            status = 0,
        )
        appMapper.insert(app)
        return app.toResponse()
    }

    fun get(tenantId: Long, id: Long): AppResponse =
        appMapper.selectById(id)?.takeIf { it.tenantId == tenantId }
            ?.toResponse()
            ?: throw BizException(ResultCode.BAD_REQUEST, "应用不存在")

    fun update(tenantId: Long, id: Long, req: AppRequest): AppResponse {
        val app = appMapper.selectById(id)
            ?: throw BizException(ResultCode.BAD_REQUEST, "应用不存在")
        if (app.tenantId != tenantId) throw BizException(ResultCode.FORBIDDEN, "无权操作")
        val updated = app.copy(name = req.name, description = req.description, iconUri = req.iconUri)
        appMapper.updateById(updated)
        return updated.toResponse()
    }

    fun delete(tenantId: Long, id: Long) {
        val app = appMapper.selectById(id)
            ?: throw BizException(ResultCode.BAD_REQUEST, "应用不存在")
        if (app.tenantId != tenantId) throw BizException(ResultCode.FORBIDDEN, "无权操作")
        appMapper.deleteById(id)
    }

    private fun App.toResponse() = AppResponse(
        id = id!!, appKey = appKey, name = name, description = description,
        iconUri = iconUri, status = status,
    )
}
```
> 注：`appMapper.insert(app)` 后 `app.id` 由 MyBatis-Plus 回填（`IdType.AUTO`）。

`controller/AppController.kt`：
```kotlin
package com.zhijin.app.controller

import com.zhijin.app.dto.AppRequest
import com.zhijin.app.dto.AppResponse
import com.zhijin.app.service.AppService
import com.zhijin.common.web.Result
import jakarta.validation.Valid
import org.springframework.web.bind.annotation.DeleteMapping
import org.springframework.web.bind.annotation.GetMapping
import org.springframework.web.bind.annotation.PathVariable
import org.springframework.web.bind.annotation.PostMapping
import org.springframework.web.bind.annotation.PutMapping
import org.springframework.web.bind.annotation.RequestBody
import org.springframework.web.bind.annotation.RequestMapping
import org.springframework.web.bind.annotation.RestController
import com.zhijin.framework.tenant.TenantContextHolder

/** 应用管理端点（/api/apps/**，JWT 保护，租户来自 JWT claim）。 */
@RestController
@RequestMapping("/api/apps")
class AppController(private val appService: AppService) {

    private val tenantId: Long get() = TenantContextHolder.getRequiredTenantId()

    @PostMapping
    fun create(@Valid @RequestBody req: AppRequest): Result<AppResponse> =
        Result.success(appService.create(tenantId, req))

    @GetMapping("/{id}")
    fun get(@PathVariable id: Long): Result<AppResponse> =
        Result.success(appService.get(tenantId, id))

    @PutMapping("/{id}")
    fun update(@PathVariable id: Long, @Valid @RequestBody req: AppRequest): Result<AppResponse> =
        Result.success(appService.update(tenantId, id, req))

    @DeleteMapping("/{id}")
    fun delete(@PathVariable id: Long): Result<Unit> {
        appService.delete(tenantId, id)
        return Result.success()
    }
}
```

- [ ] **Step 4: 运行确认通过**

Run: `cd C:\mypro\JavaProject\zhijin\zhijin-server && mvn -pl zhijin-app test -Dtest=AppServiceTest`
Expected: `2 passed`。（若 mockito 依赖缺失，加 `org.mockito:mockito-core` test 依赖，版本由 Boot BOM 管理。）

- [ ] **Step 5: Commit**

```bash
cd C:\mypro\JavaProject\zhijin
git add zhijin-server/zhijin-app/
git commit -m "feat(app): 应用 CRUD"
```

---

## Task 3: 模型配置 + 供应商 Key 加密（TDD）

**Files:**
- Create: `zhijin-server/zhijin-app/src/main/kotlin/com/zhijin/app/service/CryptoService.kt`
- Create: `zhijin-server/zhijin-app/src/main/kotlin/com/zhijin/app/service/ModelConfigService.kt`
- Create: `zhijin-server/zhijin-app/src/main/kotlin/com/zhijin/app/controller/ProviderKeyController.kt` / `ModelConfigController.kt`
- Test: `zhijin-server/zhijin-app/src/test/kotlin/com/zhijin/app/CryptoServiceTest.kt`

- [ ] **Step 1: 写失败测试（CryptoService AES-256-GCM 加解密往返）**

`CryptoServiceTest.kt`：
```kotlin
package com.zhijin.app

import com.zhijin.app.service.CryptoService
import org.junit.jupiter.api.Assertions.assertEquals
import org.junit.jupiter.api.Test

class CryptoServiceTest {

    private val crypto = CryptoService()

    @Test
    fun `AES加密后能解密还原`() {
        val plain = "sk-abcdef123456"
        val encrypted = crypto.encrypt(plain)
        assertEquals(plain, crypto.decrypt(encrypted))
    }
}
```

- [ ] **Step 2: 运行确认失败**

Run: `cd C:\mypro\JavaProject\zhijin\zhijin-server && mvn -pl zhijin-app test -Dtest=CryptoServiceTest`
Expected: 编译失败。

- [ ] **Step 3: 实现**

`service/CryptoService.kt`（AES-256-GCM，密钥来自 `MODEL_KEY_SECRET` 环境变量）：
```kotlin
package com.zhijin.app.service

import org.springframework.stereotype.Service
import java.security.MessageDigest
import java.util.Base64
import javax.crypto.Cipher
import javax.crypto.spec.GCMParameterSpec
import javax.crypto.spec.SecretKeySpec

/**
 * 模型供应商 Key 的 AES-256-GCM 加解密。
 * 加密密钥从环境变量 MODEL_KEY_SECRET 派生（SHA-256 → 32 字节），符合决策 21：平台持有加密。
 */
@Service
class CryptoService {

    private val secret: ByteArray = MessageDigest.getInstance("SHA-256")
        .digest((System.getenv("MODEL_KEY_SECRET") ?: "zhijin-dev-secret").toByteArray())

    /** 加密：输出 Base64(iv(12B) + ciphertext(含 GCM tag))。 */
    fun encrypt(plain: String): String {
        val cipher = Cipher.getInstance("AES/GCM/NoPadding")
        val iv = ByteArray(12).also { java.security.SecureRandom().nextBytes(it) }
        cipher.init(Cipher.ENCRYPT_MODE, SecretKeySpec(secret, "AES"), GCMParameterSpec(128, iv))
        val ct = cipher.doFinal(plain.toByteArray())
        return Base64.getEncoder().encodeToString(iv + ct)
    }

    /** 解密：还原 Base64 输入。 */
    fun decrypt(encrypted: String): String {
        val raw = Base64.getDecoder().decode(encrypted)
        val iv = raw.copyOfRange(0, 12)
        val ct = raw.copyOfRange(12, raw.size)
        val cipher = Cipher.getInstance("AES/GCM/NoPadding")
        cipher.init(Cipher.DECRYPT_MODE, SecretKeySpec(secret, "AES"), GCMParameterSpec(128, iv))
        return String(cipher.doFinal(ct))
    }
}
```

`service/ModelConfigService.kt`（供应商 Key 增删 + 应用模型配置存取）：
```kotlin
package com.zhijin.app.service

import com.baomidou.mybatisplus.core.conditions.query.QueryWrapper
import com.zhijin.app.entity.AppModelConfig
import com.zhijin.app.entity.ModelProviderKey
import com.zhijin.app.mapper.AppModelConfigMapper
import com.zhijin.app.mapper.ModelProviderKeyMapper
import com.zhijin.common.exception.BizException
import com.zhijin.common.web.ResultCode
import org.springframework.stereotype.Service

/** 模型配置：供应商 Key 管理（加密落库）+ 应用模型配置。 */
@Service
class ModelConfigService(
    private val keyMapper: ModelProviderKeyMapper,
    private val configMapper: AppModelConfigMapper,
    private val crypto: CryptoService,
) {

    /** 新增供应商 Key：加密后存储，明文不落库。 */
    fun addProviderKey(tenantId: Long, provider: String, name: String, plainKey: String): ModelProviderKey {
        val key = ModelProviderKey(
            tenantId = tenantId, provider = provider, name = name,
            encryptedKey = crypto.encrypt(plainKey), status = 1,
        )
        keyMapper.insert(key)
        return key
    }

    /** 读取解密后的 Key（供调用时下发，不对外返回明文）。 */
    fun getPlainKey(tenantId: Long, keyId: Long): String {
        val key = keyMapper.selectById(keyId)
            ?: throw BizException(ResultCode.BAD_REQUEST, "供应商 Key 不存在")
        if (key.tenantId != tenantId) throw BizException(ResultCode.FORBIDDEN, "无权访问")
        return crypto.decrypt(key.encryptedKey)
    }

    /** 保存应用模型配置（每个应用一条，upsert）。 */
    fun saveConfig(tenantId: Long, appId: Long, provider: String, modelName: String, providerKeyId: Long?) {
        val existing = configMapper.selectOne(
            QueryWrapper<AppModelConfig>().eq("app_id", appId).eq("tenant_id", tenantId)
        )
        val cfg = AppModelConfig(
            tenantId = tenantId, appId = appId, provider = provider,
            modelName = modelName, providerKeyId = providerKeyId,
        )
        if (existing == null) configMapper.insert(cfg) else configMapper.updateById(cfg.copy(id = existing.id))
    }
}
```

（Controller：`ProviderKeyController` `/api/model-keys` + `ModelConfigController` `/api/apps/{id}/model-config`，风格同 AppController。）

- [ ] **Step 4: 运行确认通过**

Run: `cd C:\mypro\JavaProject\zhijin\zhijin-server && mvn -pl zhijin-app test -Dtest=CryptoServiceTest`
Expected: `1 passed`。

- [ ] **Step 5: Commit**

```bash
cd C:\mypro\JavaProject\zhijin
git add zhijin-server/zhijin-app/
git commit -m "feat(app): 模型配置 + 供应商Key加密存储"
```

---

## Task 4: 版本发布（TDD）

**Files:**
- Create: `zhijin-server/zhijin-app/src/main/kotlin/com/zhijin/app/dto/PublishRequest.kt` / `AppVersionResponse.kt`
- Create: `zhijin-server/zhijin-app/src/main/kotlin/com/zhijin/app/service/PublishService.kt`
- Create: `zhijin-server/zhijin-app/src/main/kotlin/com/zhijin/app/controller/PublishController.kt`
- Test: `zhijin-server/zhijin-app/src/test/kotlin/com/zhijin/app/PublishServiceTest.kt`

- [ ] **Step 1: 写失败测试**

`PublishServiceTest.kt`（发布生成自增版本号 + 更新应用状态）：
```kotlin
package com.zhijin.app

import com.zhijin.app.entity.App
import com.zhijin.app.entity.AppVersion
import com.zhijin.app.mapper.AppMapper
import com.zhijin.app.mapper.AppVersionMapper
import com.zhijin.app.service.PublishService
import org.junit.jupiter.api.Assertions.assertEquals
import org.junit.jupiter.api.Test
import org.mockito.Mockito.`when`
import org.mockito.Mockito.mock

class PublishServiceTest {

    private val appMapper = mock(AppMapper::class.java)
    private val versionMapper = mock(AppVersionMapper::class.java)
    private val service = PublishService(appMapper, versionMapper)

    @Test
    fun `首次发布版本号为1`() {
        `when`(appMapper.selectById(1L)).thenReturn(App(id = 1L, tenantId = 1L, appKey = "app_x", name = "x", status = 0))
        `when`(versionMapper.selectCount(null)).thenReturn(0L)
        val v = service.publish(1L, 1L)
        assertEquals(1, v.versionNo)
    }
}
```

- [ ] **Step 2: 运行确认失败**

Run: `cd C:\mypro\JavaProject\zhijin\zhijin-server && mvn -pl zhijin-app test -Dtest=PublishServiceTest`
Expected: 编译失败。

- [ ] **Step 3: 实现**

`service/PublishService.kt`：
```kotlin
package com.zhijin.app.service

import com.baomidou.mybatisplus.core.conditions.query.QueryWrapper
import com.zhijin.app.dto.AppVersionResponse
import com.zhijin.app.entity.App
import com.zhijin.app.entity.AppVersion
import com.zhijin.app.mapper.AppMapper
import com.zhijin.app.mapper.AppVersionMapper
import com.zhijin.common.exception.BizException
import com.zhijin.common.web.ResultCode
import org.springframework.stereotype.Service
import java.time.LocalDateTime

/** 版本发布：draft → published，version_no 自增，生成不可变快照。 */
@Service
class PublishService(
    private val appMapper: AppMapper,
    private val versionMapper: AppVersionMapper,
) {

    fun publish(tenantId: Long, appId: Long): AppVersionResponse {
        val app = appMapper.selectById(appId)
            ?: throw BizException(ResultCode.BAD_REQUEST, "应用不存在")
        if (app.tenantId != tenantId) throw BizException(ResultCode.FORBIDDEN, "无权操作")

        val next = versionMapper.selectCount(
            QueryWrapper<AppVersion>().eq("app_id", appId).eq("tenant_id", tenantId)
        ).toInt() + 1

        val version = AppVersion(
            tenantId = tenantId, appId = appId, versionNo = next,
            workflowDsl = null, modelSnapshot = null, status = 1,
            publishTime = LocalDateTime.now(),
        )
        versionMapper.insert(version)

        appMapper.updateById(app.copy(status = 1))
        return AppVersionResponse(id = version.id!!, versionNo = version.versionNo, status = version.status)
    }
}
```
（`AppVersionResponse` 含 id/versionNo/status；`PublishController` `/api/apps/{id}/publish` 风格同前。）

- [ ] **Step 4: 运行确认通过**

Run: `cd C:\mypro\JavaProject\zhijin\zhijin-server && mvn -pl zhijin-app test -Dtest=PublishServiceTest`
Expected: `1 passed`。

- [ ] **Step 5: Commit**

```bash
cd C:\mypro\JavaProject\zhijin
git add zhijin-server/zhijin-app/
git commit -m "feat(app): 版本发布(draft→published, 版本号自增)"
```

---

## Task 5: API Key 管理（TDD）

**Files:**
- Create: `zhijin-server/zhijin-app/src/main/kotlin/com/zhijin/app/dto/ApiKeyResponse.kt`
- Create: `zhijin-server/zhijin-app/src/main/kotlin/com/zhijin/app/service/AppApiKeyService.kt`
- Create: `zhijin-server/zhijin-app/src/main/kotlin/com/zhijin/app/controller/ApiKeyController.kt`
- Test: `zhijin-server/zhijin-app/src/test/kotlin/com/zhijin/app/AppApiKeyServiceTest.kt`

- [ ] **Step 1: 写失败测试**

`AppApiKeyServiceTest.kt`（生成返回明文，存储的是哈希；吊销后校验失败）：
```kotlin
package com.zhijin.app

import com.zhijin.app.mapper.AppApiKeyMapper
import com.zhijin.app.service.AppApiKeyService
import org.junit.jupiter.api.Assertions.assertFalse
import org.junit.jupiter.api.Assertions.assertTrue
import org.junit.jupiter.api.Test
import org.mockito.Mockito.`when`
import org.mockito.Mockito.mock

class AppApiKeyServiceTest {

    private val mapper = mock(AppApiKeyMapper::class.java)
    private val service = AppApiKeyService(mapper)

    @Test
    fun `生成key返回明文, 存储为哈希, 校验通过`() {
        val resp = service.generate(1L, 1L, "客户A")
        assertTrue(resp.plainKey.startsWith("ak_"))
        // 哈希校验
        assertTrue(service.verify(1L, 1L, resp.plainKey))
    }

    @Test
    fun `吊销后校验失败`() {
        val resp = service.generate(1L, 1L, "客户A")
        service.revoke(1L, 1L, resp.id)
        assertFalse(service.verify(1L, 1L, resp.plainKey))
    }
}
```

- [ ] **Step 2: 运行确认失败**

Run: `cd C:\mypro\JavaProject\zhijin\zhijin-server && mvn -pl zhijin-app test -Dtest=AppApiKeyServiceTest`
Expected: 编译失败。

- [ ] **Step 3: 实现**

`service/AppApiKeyService.kt`（生成明文展示一次，DB 存 SHA-256 哈希）：
```kotlin
package com.zhijin.app.service

import com.zhijin.app.dto.ApiKeyResponse
import com.zhijin.app.entity.AppApiKey
import com.zhijin.app.mapper.AppApiKeyMapper
import org.springframework.stereotype.Service
import java.security.MessageDigest
import java.util.UUID

/** 应用 API Key：生成时明文返回一次，DB 存 SHA-256 哈希。 */
@Service
class AppApiKeyService(private val keyMapper: AppApiKeyMapper) {

    fun generate(tenantId: Long, appId: Long, name: String): ApiKeyResponse {
        val plain = "ak_" + UUID.randomUUID().toString().replace("-", "")
        val key = AppApiKey(
            tenantId = tenantId, appId = appId,
            keyHash = sha256(plain), name = name, status = 1,
        )
        keyMapper.insert(key)
        return ApiKeyResponse(id = key.id!!, plainKey = plain, name = name)
    }

    fun revoke(tenantId: Long, appId: Long, keyId: Long) {
        val key = keyMapper.selectById(keyId) ?: return
        if (key.tenantId != tenantId) return
        keyMapper.updateById(key.copy(status = 0))
    }

    /** 校验 API Key（供 B5 开放 API 鉴权用）。 */
    fun verify(tenantId: Long, appId: Long, plainKey: String): Boolean {
        val key = keyMapper.selectList(
            com.baomidou.mybatisplus.core.conditions.query.QueryWrapper<AppApiKey>()
                .eq("tenant_id", tenantId).eq("app_id", appId).eq("key_hash", sha256(plainKey))
        ).firstOrNull()
        return key != null && key.status == 1
    }

    private fun sha256(s: String): String =
        MessageDigest.getInstance("SHA-256").digest(s.toByteArray())
            .joinToString("") { "%02x".format(it) }
}
```

`dto/ApiKeyResponse.kt`：
```kotlin
package com.zhijin.app.dto

/** API Key 创建响应：plainKey 仅此一次返回。 */
data class ApiKeyResponse(
    val id: Long,
    val plainKey: String,
    val name: String,
)
```
（`ApiKeyController` `/api/apps/{id}/api-keys`：POST 生成、DELETE `/{keyId}` 吊销。）

- [ ] **Step 4: 运行确认通过**

Run: `cd C:\mypro\JavaProject\zhijin\zhijin-server && mvn -pl zhijin-app test -Dtest=AppApiKeyServiceTest`
Expected: `2 passed`。

- [ ] **Step 5: Commit**

```bash
cd C:\mypro\JavaProject\zhijin
git add zhijin-server/zhijin-app/
git commit -m "feat(app): API Key 管理(哈希存储, 明文一次性返回)"
```

---

## Task 6: 收尾验证

- [ ] **Step 1: 全量构建**

Run: `cd C:\mypro\JavaProject\zhijin\zhijin-server && mvn clean package`
Expected: `BUILD SUCCESS`（含全部既有 + 新增测试）。

- [ ] **Step 2: 记录实现修正**

把实现中发现的问题追加到本计划「执行修正记录」（参照 B1/B2 做法）。

- [ ] **Step 3: Commit 遗留**

```bash
cd C:\mypro\JavaProject\zhijin
git add -A
git commit -m "docs(plans): B3 追加执行修正记录"
```

---

## Self-Review 记录

- **Spec 覆盖**：§6 应用管理 ✓ · §10 数据流 A（创建→模型配置→发布→API Key）✓ · §13 决策 21（Key 加密存储）✓ · §12.1 七大原则（Service 单一职责、CryptoService 依赖倒置、租户隔离 SRP）✓。
- **占位符扫描**：无 TBD；每步含完整代码。
- **类型一致性**：`tenantId` 贯穿 Service/Controller/Mapper；`appKey`/`apiKey`/`keyHash` 命名一致；`AppVersionResponse` 在 Task 4 前后一致。

## 执行交接

B3 完成后 → **B4 编排引擎**（zhijin-orchestrator：DSL + 节点接口 + 执行器注册表 + 核心节点 + 图执行 + 显式边），届时 `app_version.workflow_dsl` 被真实填充。

---

## 执行修正记录（2026-08-19 实现期间的真实发现，均已落地并验证）

| # | 修正 | 原因 |
|---|---|---|
| 1 | mock 自增主键回填（`thenAnswer` 给 `App.id`/`AppVersion.id`/`AppApiKey.id` 赋值） | MyBatis-Plus `IdType.AUTO` 在真实 insert 后回填 id，mock 不会；service 里 `id!!` 会 NPE |
| 2 | `AppController` KDoc 路径避免 `/**` | Kotlin 把 `/**` 当嵌套注释开始，报 Unclosed comment |
| 3 | **JwtTenantFilter 锚点改为 `BearerTokenAuthenticationFilter`** | 原用 `UsernamePasswordAuthenticationFilter`（表单登录顺序 5）早于 Bearer 认证（顺序 7），过滤器在认证前运行 → `SecurityContext` 空 → 租户上下文不生效（**真实联调发现**：/api/apps 返回"缺少租户上下文"） |
| 4 | 联调时 curl 中文 body 触发 `Invalid UTF-8` | Windows 控制台 GBK 编码问题，非应用缺陷；联调用 UTF-8 文件传 body |
| 5 | `ModelConfigRequest.temperature/maxTokens` 已声明但 `saveConfig` 未消费 | V1 用实体默认值（0.7/2048），后续 B5 细化 |

> **真实联调结论**（连真 PG + 真 Nacos）：创建应用 → 添加供应商 Key（AES 加密落库，明文不在库）→ 配置模型 → 发布（versionNo 自增）→ 生成 API Key（SHA-256 哈希落库，明文一次性返回）全链路通过。
