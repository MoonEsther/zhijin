<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>com.zhijin</groupId>
    <artifactId>zhijin-server</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>zhijin-ai-client</artifactId>

  <dependencies>
    <dependency>
      <groupId>com.zhijin</groupId>
      <artifactId>zhijin-common</artifactId>
    </dependency>
    <dependency>
      <groupId>com.zhijin</groupId>
      <artifactId>zhijin-framework</artifactId>
    </dependency>
    <!-- Spring Web：RestClient 发起 HTTP 调用（版本由父 POM BOM 管理） -->
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-web</artifactId>
    </dependency>
    <!-- Jackson3 Kotlin 模块：Kotlin data class 的 JSON 反序列化（RestClient 默认 JSON 转换器依赖） -->
    <dependency>
      <groupId>tools.jackson.module</groupId>
      <artifactId>jackson-module-kotlin</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- MockWebServer：本地模拟 Python OpenAI 兼容接口 -->
    <dependency>
      <groupId>com.squareup.okhttp3</groupId>
      <artifactId>mockwebserver</artifactId>
      <version>4.12.0</version>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
